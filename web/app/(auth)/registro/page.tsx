import { RegisterForm } from '@/features/auth/components/RegisterForm';

export const metadata = { title: 'Registrarse · VocTest IA' };

export default function RegistroPage() {
  return <RegisterForm />;
}
